<Window x:Class="SmartSleep.App.Views.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Confirmation_Title}"
        Width="400"
        Height="200"
        MinWidth="350"
        MinHeight="180"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="True">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Center">
            <TextBlock Text="{Binding Message}" FontSize="14" TextWrapping="Wrap" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding CountdownText}" FontSize="16" FontWeight="Bold" Margin="0,12,0,0" HorizontalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="{DynamicResource Confirmation_Cancel}" Width="80" Height="30" Margin="0,0,12,0" Click="Cancel_Click"/>
            <Button Content="{DynamicResource Confirmation_Execute}" Width="80" Height="30" Click="Execute_Click"/>
        </StackPanel>
    </Grid>
</Window>
